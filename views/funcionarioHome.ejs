
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home CRA</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,opsz,wght@0,14..32,100..900;1,14..32,100..900&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    
</head>
<body>
    <%- include('partials/navbaradmin') %>
    <%- include('partials/msg-flash') %>
    
    <div class="profile-area">
        <div class="container">
            <div class="row">
                <% if(postagens.length == 0){ %>
                    <h2>Nenhuma Postagem no momento</h2>
                <% } %> 
                
    
    <% postagens.forEach(function(postagem){ %>
        
                
                    <div class="col-md-4">
                        <div class="card mt-4">
                            <div class="img1"> <img src="/img/teste.png" alt=""></div>
                            <div class="img2"> <img src="/postagens/<%= postagem.foto %>" alt="" style="object-fit: cover;"></div>
                            <div class="main-text">
                                <h2> <%= postagem.nome %> </h2>
                                <p> Última Localidade: <br> <%= postagem.ultima_localidade %></p>
                                <a class="btn btn-primary" href="/postagem/detalhes/<%=postagem.id%>"> Mais Informações</a>
                                <a class="btn btn-success" href="/postagem/aprovar/<%=postagem.id%>">Aprovar</a>
                                <a class="btn btn-danger" href="/postagem/recusar/<%=postagem.id%>"> Recusar </a>
                                <br>
                                <% if(postagem.aprovar == 0){ %>
                                    <span class="badge bg-warning text-dark">Status: Não Informado</span>
                                <% } %>
                                <% if(postagem.aprovar == 1){ %>
                                    <span class="badge bg-success" style="margin-top: 10px;">Status: Aprovado</span>
                                <% } %>
                                <% if(postagem.aprovar == 2){ %>
                                    <span class="badge bg-danger">Status: Recusado</span>
                                <% } %>
                            </div>
                        </div>
                    </div>
                
                
    <% }) %>
    <!-- as divs estao fora do loop para os cards ficarem um do lado do outro -->
        </div> 
    </div>
</div>
<div class="content">
    <a href="/apoia"><button class="fixed-btn btn-1"> Apoia-se </button></a>
    <a href="/sobre"><button class="fixed-btn btn-2">Sobre</button></a>
  </div>
    

                
                
     <style>
   .content {
    background-color: #FFD1DC;
  padding: 20px;
}

.fixed-btn {
  position: fixed;
  padding: 10px 20px;
  font-size: 16px;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  color: white;
  text-align: center; /* Centraliza o texto */
  width: 120px; /* Largura fixa para ambos os botões */
}

.btn-1 {
  bottom: 100px;
  right: 20px;
  background-color: #567ebb;
}

.btn-2 {
  bottom: 20px;
  right: 20px;
  background-color: #567ebb;
}

.fixed-btn:hover {
    font-weight: bold;
    transition: 0.3s;
  opacity: 0.9;
}
        
        html{
            background-color: #FFD1DC;
        }
        .profile-area{
            background-color: #FFD1DC;
            padding: 80px 0;
        }
        .card{
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
            overflow: hidden;
            border-radius: 15px;
        }
        .img1 img{
            height: 200px;
            border-top-right-radius: 15px;
            border-top-left-radius: 15px;
            width: 100%;
        }
        .img2 img{
            position: relative;
            display: block;
            margin-left: auto;
            margin-right: auto;
            z-index: 1;
            width: 140px;
            height: 140px;
            border-radius: 50%;
            border: 7px solid #fff;
            margin-top: -80px;
        }
        .main-text{
            padding: 30px 0;
            text-align: center;
        }
        .main-text h2{
            text-transform: uppercase;
            font-weight: 900;
            font-size: 20px;
            margin: 0 0 20px;
        }
        .main-text p {
            font-size: 16px;
            padding: 0 35px;
        }
        
     </style>           
    <script>
        if(document.getElementById("teste") != undefined){
            const login = document.getElementById("login")
            const cadastro = document.getElementById("cadastro")
            const postagem = document.getElementById("postagem")
            login.setAttribute('class', 'hidden')
            cadastro.setAttribute('class', 'hidden')
           

        }else{
            
        }
    </script>
</body>

 

<script src="/js/bootstrap.min.js"></script>
</html>